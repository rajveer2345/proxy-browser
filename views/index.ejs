<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Search</title>
    <style>
      /* Reset body and center content */
      body,
      html {
        height: 100%;
        margin: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
      }

      /* Centered container */
      .container {
        text-align: center;
        max-width: 600px;
        width: 90%;
      }

      /* Input box styling similar to Google */
      .search-box {
        display: flex;
        align-items: center;
        border: 1px solid #dcdcdc;
        border-radius: 24px;
        padding: 12px 20px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        background-color: #fff;
      }

      /* Style input */
      .search-box input[type="text"] {
        border: none;
        outline: none;
        flex: 1;
        padding: 10px;
        font-size: 16px;
        color: #333;
      }

      /* Style submit button */
      .search-box button {
        background-color: #4285f4;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 24px;
        font-size: 16px;
        cursor: pointer;
      }

      /* Button hover effect */
      .search-box button:hover {
        background-color: #357ae8;
      }

      /* Heading styling */
      h1 {
        font-size: 24px;
        color: #333;
        margin-bottom: 40px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Enter a URL to Browse</h1>
      <form action="/browse" method="get">
        <div class="search-box">
          <input type="text" name="url" placeholder="Enter URL" required />
          <button type="submit">Go</button>
        </div>
      </form>
    </div>
  </body>
</html>
/////////////////// -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Browser</title>
  </head>
  <body style="margin: 0%;">
    <main
      style="
        height: 100vh;
        width: 100%;
        
        display: flex;
        align-items: center;
        justify-content: center;
      "
    >
      <form style="width: 100%;" action="/browse" method="get">
        <div style="display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 16px; width: 100%;">
          <input
          style="height: 2.5rem; font-size: 1.2rem; width: 80%; border-radius: 10px; border: 2px solid lightgrey; padding: 0px 12px; max-width: 500px;"
            type="text"
            name="url"
            placeholder="Enter URL"
            required
            autofocus
          />

          <button style="background-color: black; color: white; height: 2.5rem; width: 6rem; border-radius: 4px; font-size: 16px;" type="submit">GO</button>
        </div>
      </form>
    </main>
  </body>
</html>
